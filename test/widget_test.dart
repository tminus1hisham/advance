import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder – StorageService requires async init.
    // Full widget tests will be added in a future phase.
    expect(true, isTrue);
  });
}
